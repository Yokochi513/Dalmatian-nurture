# AGENT.md

このリポジトリで作業するエージェントが守るルール。

## ルール

1. 各決定事項はADRとして`docs/adr/`内に1つずつ必ず記録する
2. ただし、1つのモジュールの内部の決定は`docs/module_design/`に記録する（[ADR 0041](docs/adr/0041-module-design-docs.md)）。モジュールの概要は`<モジュール名>.md`、部品ごとの設計は`<モジュール名>/<部品名>.md`に書く（[ADR 0043](docs/adr/0043-module-design-per-component.md)）。ほかのモジュールに影響する場合や、どちらか迷う場合はADRにする
3. 設計を行うときは、必ず実装より先に設計書（`docs/module_design/`）を作成・更新する。設計書に書いていない設計でコードを書かない。実装中に設計を変えた場合は、設計書も同じコミットで更新する
